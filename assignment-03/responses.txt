An IP address is similar to a phone number or mail address. You need to know the mail address to send a package, not the person. A DNS essentially finds the domain name you search on a browser and translates that into its corresponding IP address.
A cookie is a way for websites to remember you information once you have visited it such as logins and passwords.
The three main parts of the URL are the protocol, the domain, and the path.
